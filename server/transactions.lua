function BankingTransactions(bill)
    -- data passed to this function is as follows
    -- amount = amount (bill amount) (bill.amount)
    -- from = source (Who issued the bill) (bill.from)
    -- to = target (Bill Target (Session ID)) (bill.to)
    -- job = job (Job of the issuer) (bill.job)
    -- name = name (Name of the issuer) (bill.name)
end